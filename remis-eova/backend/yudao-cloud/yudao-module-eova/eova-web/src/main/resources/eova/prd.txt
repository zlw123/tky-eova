# ★ 档位 = PRD × KingbaseES（生产档）—— 见 docs/DES-010-R1-eova-deploy-profile-selection.md
# 与 dev.txt 的差异**闭合在 8 个坐标键 + env + file.dir.base + devMode** 上（有判据钉住）。
# ★ 与旧模板 meta-eova/eova/demo/src/main/resources/eova/prd.txt 的**显式差异（仅此一处）**：
#   旧模板误留 devMode=true，而 configConstant 自己就会为此告警（"生产环境开启开发者模式…避免不可逆后果"）
#   ⇒ 本档取 devMode=false；其余键（如 file.dir.base=/data/eova）沿用旧模板。
# 部署前必须替换：数据源坐标、凭据、file.dir.base（生产真实路径）。
# 用法：java -Deova.prop=eova/prd.txt ... cn.eova.web.EovaWebApplication
#初始化启动配置
#----------------------------------
#开发模式：输出JFinal日志
devMode=false

#环境标识：开发环境=DEV,测试环境=TEST,预发布环境=PRE,正式环境=PRD
env=PRD

#上传根目录
file.dir.base=/data/eova
#导入Excel上传目录(格式:/xxx)
file.dir.excel = /excel
#产品上传目录
file.dir.product = /product


#事务级别(MYSQL=4,Oralce=2)
db.transaction_level=4

#JDBC配置
db.datasource=eova,main

eova.url=jdbc:kingbase8://base.platform:54321/eova_meta
eova.user=system
eova.pwd=Rmtlwrm@@2026
eova.driver=com.kingbase8.Driver

main.url=jdbc:kingbase8://base.platform:54321/demo
main.user=system
main.pwd=Rmtlwrm@@2026
main.driver=com.kingbase8.Driver

db.pwd.encrypt=false

#定时调度
job.enable=false

###################开发环境配置
#应用状态查看令牌
app.status.token = eova


# 应用名
#app.name=运营支撑管理系统
# 应用版权信息
#app.copyright=© 2015-2025 EOVA.CN
# 应用LOGO
#app.logo=/eova/_view/index/meta.png
# 自定义登录页
#app.login.page=/_view/login/login.html

#默认登录密码
dev.login_id = eova
dev.login_pwd = 000000

dev.domain=http://meta.dev.eova.cn

# 登录会话有效期(分钟)
login.user.session=12000
