<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.eova</groupId>
        <artifactId>yudao-cloud</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>yudao-module-eova</artifactId>
    <packaging>pom</packaging>
    <name>yudao-module-eova</name>
    <description>EOVA 模块聚合（eova-compat / eova-core 先行；biz/api 见 LC-001 Deferred）</description>

    <modules>
        <!-- 非业务兼容适配层：模板接缝、旧工具、序列化语义等宿主适配（DES-002-R4 §4） -->
        <module>eova-compat</module>
        <!-- 数据访问适配层：唯一允许接触数据源与事务的边界（DES-002-R4 §4） -->
        <module>eova-db-adapter</module>
        <module>eova-core</module>
    </modules>
</project>
